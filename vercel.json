{
  "version": 2,
  "builds": [
    { 
      "src": "app.py", 
      "use": "@vercel/python",
      "config": {
        "maxDuration": 60,  // Timeout in seconds
        "memory": 512       // Memory allocation in MB
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/app.py" }
  ]
}
